"""Completed, trusted & interesting scripts for persona extraction research.

Each experiment is a standalone Python script that defines its own configuration
and calls the component libraries directly.

Scripts should only be moved into this repository when we are happy with them, 
and when all components they call are in src/.
Subfolders within here should group together similar types of experiments (e.g. folder 
for training LoRAs, folder for evaluating the composition of LoRAs, etc.)
"""
