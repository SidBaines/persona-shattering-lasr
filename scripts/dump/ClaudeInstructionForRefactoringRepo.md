Please can you help me think about a refactor of the overall plan for running experiments in this codebase. Previously, I'd thought that we'd have something like "A pipeline, which runs components in sequence, and everything we want to do can be defined by a high level config which tells it which Pieces of the code have the whole pipeline to run and with what config". But I've been thinking, and I think I want to make the following adjustments:\
\
- Think we want a setup where actually each component is just called within a script, script defines the experiment, they can run any stages they want in whatever order they want, repeat stages, etc.
- Currently we have prescriptive "they have to run in this order" setup, we don't want this probably long run
- So components should look more like "inference" (which can be local or OpenRouter), "training" (which could be local or Tinker, could be SFT or DPO), "logging" (which could e.g. be local, wandb), "metrics", "LLM-judge", "LoRA operations", etc. - just reusable bits of code\\
\
So there's no high-level single campaign which has to run things virtually. The idea is that we'll be able to just write scripts and call each of these things, and then we can run multiple of them repeatedly in the same script. We can go from one to the other and back again. It's just a lot more flexible. And then also, components can call other components much more easily. So, for example, if we want to do metrics, this could be like just code-based metrics or it could call an LLM to judge. If so, then the LLM judge could use the inference module for example.\
\
I don't want you to make any actual changes here, but what I want you to do is look through the codebase and plan how you would have to change this to make this possible. I want to keep ideally like each component has the CLI and the README in it so that it's easy for humans or LLM agents to piece these together to write a script for a given experiment that we want to run - So I guess what I really want to change is remove the pipeline and the high-level config stuff. Does that make sense? Can you make a plan for how to do this?