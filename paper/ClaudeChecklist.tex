List of things for claude to check through:

@Claude, if you are going through this list and are in any way unsure as to the meaning of one of the points, please ask the user.

- make sure that appendices appear in the order that they're referenced in main body
- Check that occurrences of "TRAIT" where discussing the benchmark are \texttt{TRAIT} - make sure not to overdo. Applies to other benchmarks too
- Nice to have: OCEAN macro which has each letter as the ocean colours
- behavior -> behaviour (use british spelling consistently). Should probably also flag (but don't just replace) other american spellings. However it should be LoRA adapter, not adaptor
- check all the cross-section references (figures, sections)
- references point to the right papers
- arrows instead of +/- for amplifier/suppressor
- vanton occurrences should be gone
- for figure references stick to Figure x (rather than fig. x)
- consistent format of: baseline (instead of base), act. capping, @ scale x, 
- make sure we refer to llama in a consistent format
- flag any duplicated sections (either actual complete duplicates or just bits that look the same/similar)
- should not refer to files from repo - give some examples or upstream datasources if needed. for code files we should not mention at all - we can make an invisible comment if needed.
- maybe avoid long dashes

- any leftover TODOs
