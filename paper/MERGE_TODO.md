# Post-merge todo: re-apply local edits if they got overwritten

This file tracks edits made on `sid/paper-updates-1-20260506` to the local
`paper/` tree before the overleaf merge of 2026-05-07. The merge treats
`paper_overleaf/paper/` as source of truth for *all text and images*, so
anything in this list may have been overwritten by an older overleaf
revision and needs reapplying. Delete this file once each item is checked
or confirmed already-present in the merged version.

Pre-merge HEAD: `b9f1251b` (sid/paper-updates-1-20260506).

## 1. TIDE rename in introduction.tex

`paper/sections/introduction.tex` line 38 used to say
"Thoroughness, Exuberance, Warmth, and Didacticism" — replaced with the
TIDE list:

> This recovers stable, interpretable latent dimensions --- Tone, Initiative, Didacticism, and Epistemic Caution (acronym **TIDE**).

If overleaf still has the old names, rewrite that sentence.

## 2. Unsupervised section — main figure bar chart

`paper/sections/unsupervised.tex` was rewritten on this branch to:

- Use TIDE factor names ("Tone", "Initiative", "Didacticism",
  "Epistemic Caution") in the four-factor list (line 25 area). Acronym
  spelled out as **TIDE**.
- Reference Figure `fig:4-lora-shifts` which is the **bar chart**
  (`figures/unsupervised/fig_4_2_6_lora_initiative_bars.pdf`), not a
  heatmap. Bar chart shows Initiative-amp, Initiative-sup, **and**
  the OCEAN-def Control LoRA (grey bars).
- Quote magnitudes for Initiative-amp: `+1.5σ` headline, rising to
  `+2.0σ` on the headroom tertile (was previously `+1.8σ`).
- Caption explicitly mentions the grey "Control" bars and explains
  what they are (negative-control LoRA trained against generic OCEAN
  trait definitions).
- All four factor names in the cross-model congruence sentence point
  at the TIDE labels, not Warmth/Pedagogy/Hedging.

If overleaf has an older heatmap-only main figure or pre-TIDE prose,
copy the post-merge equivalent of `paper/sections/unsupervised.tex`
back from `git show b9f1251b:paper/sections/unsupervised.tex`.

## 3. fa_factors appendix — LoRA validation table & narrative

`paper/appendices/fa_factors.tex` was substantially refreshed:

- Table `tab:4-2-lora-shifts` now includes a fifth row ("Control"), and
  the Initiative ↑ / Initiative ↓ rows use **v2** paired-DPO numbers
  (Initiative ↑: naive `+1.54`, middling `+1.50`, headroom `+1.97`;
  Initiative ↓: naive `-0.44`, middling `-0.45`, headroom `-0.46`).
- Caption says "all five LoRAs use n=1000 validation personas; the
  Initiative LoRAs are the v2 paired-DPO retrain".
- Three numbered observations (was two): added a third paragraph
  about the OCEAN-def control floor and what the trait LoRAs clear
  it by.
- Headroom-effect callouts updated: `+1.52→+1.54` (naive) and
  `+1.81→+1.97` (headroom) on Initiative ↑.
- All factor names use TIDE labels (Tone / Didacticism / Epistemic
  Caution), no Warmth / Pedagogy / Hedging in prose.
- Per-factor item subsubsections F0-F3 use TIDE headings.

If overleaf's `fa_factors.tex` has older numbers / no control row /
older factor names, recover from
`git show b9f1251b:paper/appendices/fa_factors.tex`.

## 4. MANIFEST.md

`paper/figures/MANIFEST.md` had entries for the new
`figures/unsupervised/` outputs (initiative bars, lora_shifts heatmap
variants, residualized refit fig, variance decomp fig). Will be
rebuilt fresh after the merge from a scan of `\includegraphics` +
attached `% Generated by:` lines, so this entry is FYI only.

## How to recover any specific local version of a file

```bash
git show b9f1251b:paper/sections/introduction.tex > /tmp/local_intro.tex
git show b9f1251b:paper/appendices/fa_factors.tex > /tmp/local_fa.tex
git show b9f1251b:paper/sections/unsupervised.tex > /tmp/local_unsup.tex
```

Then `diff` against the merged-in overleaf version and cherry-pick.

## 5. Comment pointers we couldn't auto-resolve from pre-merge git

The merge auto-applied `% Generated by:` / `% Data:` comments above
\includegraphics where the figure path matched between pre-merge
local (`b9f1251b`) and merged overleaf. The figures below have new
filenames in overleaf and need their pointer comments added by hand:

### Templates (single comment pair belongs above the `\newcommand`):

- `paper/appendices/activation_capping.tex` — `\actcapfigdir/...` template figs
- `paper/appendices/downranking.tex` — `\downrankfigdir/trait_sweep_*` template
- `paper/appendices/instruct_tuned_base_interp.tex` — `\interpfigdir/trait_sweep_*` template
- `paper/appendices/ocean_results.tex` — `\oceanfigdir/{trait_sweep,judge_sweep,mmlu_breakdown,gsm8k,truthfulqa}_*` templates

For each, identify the single plotting script the template fans out to
(probably `src_dev/visualisations/paper_appendix_*.py` or similar) and
add one `% Generated by:` / `% Data:` block above the `\newcommand`.

### One-off figures that are new in the overleaf version:

- `paper/sections/introduction.tex:7` — `figures/main/pipeline.pdf`
  (pre-merge had `pipeline.png` with comment "% Generated by: N/A
  --- hand-authored HTML at figures/main/persona_figure-2.html, rendered
  via headless Chrome + pdfcrop / % Data: N/A --- hand-drawn"
  — likely the same source if pdf is just a pdf'd version of the png).
- `paper/sections/supervised.tex:94,100,147,198` — `fig_3_3_1_c_minus_n_plus_*`,
  `fig_residuals_distribution-8.pdf`, `fig_8_apologize_coconot.pdf`
- `paper/appendices/flattened_weight_space.tex` — `pca_4_5.pdf`, `pca_8_9.pdf`
  (`pca_2_3.pdf` already has a comment via the `pca_0_1.pdf` block).
- `paper/appendices/heatmaps_residuals.tex:4` — `fig_residuals_heatmap.pdf`
- `paper/appendices/ocean_results.tex:95,103` — control trait_sweep / mmlu_breakdown
- `paper/appendices/other_families.tex:37` — `figures/appendix/gemma27b.pdf`

The corresponding `% Data:` paths for many of these can probably be
recovered by grepping the relevant plotting script's docstring or
the data fingerprint convention in `src_dev/evals/cell_sweep/`.

## 6. Pre-existing build warning (not merge-introduced)

The merged build emits one undefined-reference warning:

> `LaTeX Warning: Reference \`sec:training-loras' on page 1 undefined`

caused by `paper/sections/supervised.tex` line 5 having the
`\subsection{Training trait adapters}\label{sec:training-loras}` line
commented out, while `paper/sections/discussion.tex:2` still references
`\cref{sec:training-loras}`. Either uncomment the subsection in
supervised.tex or rewrite the discussion sentence.

## 7. MANIFEST.md rebuild

`paper/figures/MANIFEST.md` is stale relative to the merged figure set.
Rebuild from a fresh scan of all `\includegraphics` + their attached
`% Generated by:` lines once item 5 is resolved.
